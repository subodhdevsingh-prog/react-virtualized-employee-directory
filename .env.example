# Environment variables for the Employee Directory demo.
# This app needs no secrets to run; the file documents the Vite env pattern.
# Copy to `.env` and uncomment to override defaults.

# VITE_APP_TITLE=Employee Directory
